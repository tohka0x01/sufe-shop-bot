services:
  app:
    build: .
    container_name: shopbot-Grant_Di
    env_file:
      - .env.production
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./templates:/app/templates:ro
      - ./static:/app/static:ro
      - ./logs:/app/logs
      - ./data:/app/data
    ports:
      - "10000:9147"
    networks:
      - 1panel-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9147/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

networks:
  1panel-network:
    external: true